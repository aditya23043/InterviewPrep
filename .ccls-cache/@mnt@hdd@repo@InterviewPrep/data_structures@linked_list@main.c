#include "linked_list.h"

/* INFO
 * Access: O(n)
 * Insert: O(n)
 * Search: O(n)
 * Delete: O(n)
 *
 * MSP: Used when it is difficult to allocate contiguous blocks of memory
 */

int main(void) {

    struct Node *root;
    initialize(root);

    /* add(root, 5); */
    /* add(root, 6); */
    /* add(root, 7); */
    /* add(root, 7); */
    /* add(root, 5); */

/*     print(root); */

    /* printf(root); */
    clean(root);
}
